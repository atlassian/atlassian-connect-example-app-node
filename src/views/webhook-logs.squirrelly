{{@extendsFile('./layout.squirrelly')}}
    {{#title}}
    Webhook logs
    {{#content}}
    {{@includeFile('./partial/sidebar.squirrelly') /}}
    <div>
        {{@includeFile('./partial/back-nav.squirrelly') /}}
        <div id="webhooks" data-page-content="{{ it.pageContent.toString() }}"></div>

        <div class="logs-title">
            <h2>Example logs:</h2>
            <div>
                <a class="aui-button" onclick="AP.navigator.reload();">
                    <span class="aui-icon aui-icon-small aui-iconfont-refresh">Refresh</span>
                    <span>Refresh</span>
                </a>
            </div>
        </div>

        <p>Whenever you create, update or delete any issue, the events are being captured using webhooks and can be seen below.</p>
        <p>You can check out the <code>atlassian-connect.json</code> to view the webhooks.</p>

        <p>Whenever you create, update or delete any issue, the events are being captured using webhooks and can be seen below.</p>
        <p>You can check out the <code>atlassian-connect.json</code> to view the webhooks defined.</p>

        <div class="logs-container">
            {{@if(it.logs.length)}}
                {{@each(it.logs) => log}}
                    <pre>{{log.message}} -----> {{log.data}}</pre>
                {{/each}}
            {{#else}}
                <pre>No logs yet!</pre>
            {{/if}}
        </div>
    </div>
{{/extendsFile}}
